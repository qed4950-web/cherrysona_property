# 없는데이터로해보기1차

아래 내용은 **업로드된 CSV들만**을 전제로, End-to-End 기획서를 “전처리 → 피처(VC/RO/Persona) 설계 → 파이프라인(Pandas+MySQL) → 대시보드(Streamlit)” 순서로 **다시 구성**한 초안입니다.

(페르소나 4종: **연금형, 상업건물, 실거주 성장, 시세차익형**)

---

# 1) 데이터 전처리(우선 작성)

## 1.1 공통 스키마 통합

- **입력 파일(예시)**
    - `아파트(매매)_실거래가_YYYYMMDD.csv`
    - `아파트(전월세)_실거래가_YYYYMMDD.csv`
    - `오피스텔(매매)_실거래가_YYYYMMDD.csv`
    - `오피스텔(전월세)_실거래가_YYYYMMDD.csv`
    - `상업업무용(매매)_실거래가_YYYYMMDD.csv`
    - (+ 연립/단독 등은 이번 1차 범위에서 **참고 보류**하거나 동일 절차로 확장)
- **표준 컬럼 설계(타입/예시)**
    - 지역키: `시도`, `시군구`, `읍면동`, `법정동코드` (string)
    - 물건정보: `건축년도`(int), `층`(int), `전용면적_㎡`(float)
    - 거래기본: `거래금액_만원`(int), `보증금_만원`(int), `월세_만원`(int, 월세형만), `계약년월`(int: YYYYMM), `계약일`(int: DD), `해제사유발생일`(date|NaT)
    - 파생공통: `계약일자`(date), `연도`, `월`, `분기`, `면적_평`(㎡×0.3025), `가격_per_㎡`(만원/㎡), `가격_per_평`(만원/평)

## 1.2 값 정규화 & 클린징

- **문자 혼입 제거**: 금액/면적/층에 포함된 콤마·공백·문자 제거 → 수치형 변환.
- **날짜 생성**: `계약년월`+`계약일` → `계약일자`(유효성 검증: 존재하지 않는 날짜는 월 말로 보정).
- **이상치 처리**:
    - 면적·가격: 군집별(IQR 또는 z-score)로 **면적×준공연도×유형** 버킷 내 상·하위 1% 컷/윈저라이즈.
    - 층: 극단값(예: ±50층 초과) 검토 후 제거/보정.
- **결측치**:
    - `건축년도` 결측: 동일 단지/법정동 평균·최빈 보정(없으면 NaN 유지, 모델 입력 시 결측 인코딩).
    - `전용면적_㎡`, `거래금액_만원` 결측 행 제외(핵심지표 불가).
- **중복 제거**: `(지역키+계약일자+면적+금액)` 근접중복은 한 건만 유지.
- **지역코드 정합**: 법정동코드 최신 테이블(사전 준비)로 매핑 → `광역시/구/동` 표준화.

## 1.3 전월세 환산가/수익률 규칙(파라미터화)

- **환산식**: `환산가_만원 = 보증금_만원 + 월세_만원 × K`
    - `K`는 설정값(기본 **100** 권장, 운영에서 지역금리/전환율 기반으로 조정 가능).
- **Yield(연 수익률, %) 예시**
    - `연임대수입_만원 = 월세_만원 × 12`
    - `추정매입가_만원`: (① 매매가 존재 시 그 값, ② 없으면 환산가 사용)
    - `Yield = (연임대수입_만원 / 추정매입가_만원) × 100`

## 1.4 트렌드/모멘텀·거래량/취소율·변동성 지표

- **거래량(월별)**: `count(*) by 지역×유형×YYYY-MM`
- **모멘텀**: `MOM_거래금액 = (최근3개월 평균가격_per_㎡ / 이전3개월 평균) - 1`
- **취소율**(proxy): `해제사유발생일 존재 건 / 해당월 계약건`
- **변동성**: `가격_per_㎡` 월별 표준편차/변동계수(CV)
- **공실 proxy(임대)**: 전월세 **거래량 추세 하락** 및 환산가 하방 변동 확대

---

# 2) 파일 × 열 × VC/RO/Persona: Feature 재정의

## 2.1 아파트(매매)

- **VC (Value & Choice)**
    - `거래금액_만원`, `전용면적_㎡` → **㎡당가격/평당가**
    - `건축년도`, `층` → **가격예측/랭킹(신축·중층 프리미엄)**
    - `계약년월`, `계약일` → **월별 집계, 모멘텀**
- **RO (Risk & Ops)**
    - `계약년월`, `계약일` → **거래량 트렌드(유동성)**
    - `해제사유발생일` → **취소율**
    - `거래금액_만원` → **가격 변동성**
- **Persona**
    - **연금형**: `계약년월/계약일/해제사유발생일` → 안정성·무취소
    - **상업건물(참고)**: `거래금액/전용면적` → 벤치마크
    - **실거주 성장**: `전용면적, 건축년도, 층, 거래금액`
    - **시세차익형**: `계약년월, 거래금액, 전용면적` → **모멘텀·z-score**

## 2.2 아파트(전월세)

- **VC**
    - `보증금, 월세` → **전월세 환산가, Yield**
    - `전용면적, 거래금액(매입가)` → **매입 대비 수익률**
- **RO**
    - `계약년월/계약일` → **DOM proxy(거래량)**
    - `해제사유발생일` → **분쟁/해제율**
- **Persona**
    - **연금형**: `보증금, 월세` → **세후 현금흐름 안정**
    - **실거주 성장**: `보증금, 월세, 거래금액` → **렌트 vs 매입**
    - **시세차익형**: `보증금, 월세` → **밸류에이션 괴리율**

## 2.3 오피스텔(전월세)

- **VC**
    - `보증금, 월세` → **환산 Yield**
    - `거래금액, 전용면적` → **수익률**
- **RO**
    - `계약년월/계약일` → **거래량 추세 = 공실 proxy**
    - `해제사유발생일` → **안정성 신호**
- **Persona**
    - **연금형**: `보증금, 월세` → **안정적 현금흐름**
    - **상업건물**: `거래금액, 전용면적` → **준상업 벤치마크**
    - **시세차익형**: `계약년월, 거래금액` → **단기 변동성 플레이**

## 2.4 상업업무용(매매)

- **VC**
    - `거래금액, 전용면적` → **㎡당가격/평당가**
    - `건축년도, 층` → **Value-add 포인트(리모델/리포지셔닝)**
- **RO**
    - `계약년월/계약일` → **거래량 둔화/유동성**
    - `해제사유발생일` → **리스크 이벤트**
- **Persona**
    - **상업건물**: `거래금액, 전용면적, 건축년도, 층` → **핵심 판단**
    - **연금형(참고)**: `거래금액` → 일부 안정형
    - **시세차익형**: `계약년월, 거래금액` → **단기 차익 탐색**

## 2.5 요약 매핑 표

```
파일명          | VC 활용 열                                | RO 활용 열                          | 연금형                          | 상업건물                              | 실거주 성장                               | 시세차익형
아파트(매매)     | 거래금액, 전용면적, 건축년도, 층, 계약년월     | 계약년월, 계약일, 해제사유, 거래금액       | 계약년월, 계약일, 해제사유           | 거래금액, 전용면적                        | 전용면적, 건축년도, 층, 거래금액            | 계약년월, 거래금액, 전용면적
아파트(전월세)    | 보증금, 월세, 전용면적, 거래금액(매입가)       | 계약년월, 계약일, 해제사유              | 보증금, 월세                      | —                                     | 보증금, 월세, 거래금액(렌트 vs 매입)         | 보증금, 월세
오피스텔(전월세)  | 보증금, 월세, 거래금액, 전용면적              | 계약년월, 계약일, 해제사유              | 보증금, 월세                      | 거래금액, 전용면적                       | —                                         | 계약년월, 거래금액
상업업무용(매매)  | 거래금액, 전용면적, 건축년도, 층              | 계약년월, 계약일, 해제사유              | 거래금액(참고)                    | 거래금액, 전용면적, 건축년도, 층           | —                                         | 계약년월, 거래금액

```

---

# 3) 파이프라인 설계: Pandas + MySQL

## 3.1 MySQL 스키마(요약)

- **stg_**: 원천 CSV 적재(원본형)
    - `stg_apt_trade`, `stg_apt_lease`, `stg_offi_lease`, `stg_comm_trade`
- **dwh.dim_**: 차원
    - `dim_geo(법정동코드, 시도, 시군구, 읍면동, …)`
    - `dim_time(일자, 연, 월, 분기, YYYYMM, …)`
    - `dim_building(유형, 건축년도, …)`
- **dwh.fact_transactions**
    - 키: `(trans_id PK)`
    - 외래키: `geo_id`, `time_id`, `building_id`
    - 값: `유형`, `전용면적_㎡`, `거래금액_만원`, `보증금_만원`, `월세_만원`, `계약일자`, `해제여부`, 파생지표(㎡당가, 환산가, Yield, 모멘텀, 변동성 등)

> 인덱스: (유형, YYYYMM), (geo_id, YYYYMM), (유형, geo_id, 계약일자)
> 
> 
> **파티션**: `YYYYMM` 단위 권장
> 

## 3.2 Pandas 적재 & 정제(예시 스니펫)

```python
import pandas as pd
from datetime import datetime

K = 100  # 전월세 환산계수(설정값)

def clean_num(x):
    if pd.isna(x): return None
    return float(str(x).replace(',', '').strip())

def build_date(yyyymm, dd):
    y = int(str(yyyymm)[:4]); m = int(str(yyyymm)[-2:]); d = int(dd) if pd.notna(dd) else 1
    # 유효성 보정
    try: return pd.Timestamp(y, m, d)
    except:
        # 월말로 보정
        nxt = pd.Timestamp(y, m, 1) + pd.offsets.MonthEnd(0)
        return nxt

df = pd.read_csv('아파트(전월세)_실거래가_YYYYMMDD.csv')
for c in ['거래금액(만원)','보증금(만원)','월세(만원)','전용면적(㎡)','층','건축년도']:
    if c in df.columns: df[c] = df[c].apply(clean_num)

df['계약일자'] = df.apply(lambda r: build_date(r['계약년월'], r['계약일']), axis=1)
df['가격_per_㎡'] = (df['거래금액(만원)'] / df['전용면적(㎡)']).replace([pd.NA, pd.NaT], None)
df['환산가_만원'] = (df.get('보증금(만원)',0) + df.get('월세(만원)',0) * K)
df['Yield_%'] = (df.get('월세(만원)',0)*12 / df[['거래금액(만원)','환산가_만원']].max(axis=1)) * 100

```

> 이후 to_sql()로 stg_* 적재 → SQL로 fact/dim 생성/갱신(뷰 또는 머지)
> 

## 3.3 MySQL DDL(개요)

```sql
CREATE TABLE dwh.fact_transactions (
  trans_id BIGINT AUTO_INCREMENT PRIMARY KEY,
  type VARCHAR(16),              -- apt_trade / apt_lease / offi_lease / comm_trade
  geo_id INT, time_id INT, building_id INT,
  area_m2 DECIMAL(10,2), floor INT, built_year INT,
  price_10k INT, deposit_10k INT, rent_10k INT,
  deal_date DATE, cancel_flag TINYINT(1),
  p_per_m2 DECIMAL(12,2), conv_10k INT, yield_pct DECIMAL(6,2),
  mom_pct DECIMAL(6,3), vol_cv DECIMAL(6,3),
  KEY idx_type_ym (type, time_id),
  KEY idx_geo_ym (geo_id, time_id),
  KEY idx_type_geo_date (type, geo_id, deal_date)
);

```

---

# 4) Streamlit 대시보드 1차 기획

## 4.1 페이지/탭 구조

- **페이지 A. Overview (요약 KPI)**
    - **카드**: 지역·유형 선택값에 따른
        - 평균 ㎡당가 / 평당가
        - 거래량(최근 3/12개월) & **MOM/YOY**
        - 취소율(해제사유)
        - 변동성(CV)
        - 임대형: **Yield** 평균
    - **차트**:
        - 가격_per_㎡ 추세(월별)
        - 거래량 추세(월별)
        - 변동성 추세(CV 월별)
    - **워딩**:
        - “최근 3개월 평균 ㎡당가격은 **{X}만원**, 전월 대비 **{±Y%}** 변동입니다.”
        - “거래 취소율은 **{Z%}**로 최근 12개월 평균 대비 **{상승/하락}**했습니다.”
- **페이지 B. VC 랩(추천/랭킹)**
    - **필터**: 지역, 유형, 면적범위, 건축년도, 층, 예산
    - **랭킹 테이블**: ㎡당가, 모멘텀, 신축/층 프리미엄, (임대형은 Yield)로 **가중합 스코어**
    - **산점도**: 면적 vs ㎡당가(색=건축년도, 사이즈=층)
    - **워딩**:
        - “선택 조건에서 **상위 10개** 후보를 추천합니다. 스코어는 **가격 경쟁력(40%) + 품질 프리미엄(30%) + 모멘텀(30%)** 가중으로 계산됩니다.”
- **페이지 C. RO 보드(리스크&운영)**
    - **카드**: 취소율, 거래량 둔화(최근3→최근12개월), 변동성 상위 구역
    - **차트**:
        - 취소율 히트맵(지역×월)
        - 거래량 추세와 3M/12M 이동평균
        - 가격 변동계수(CV) 막대
    - **워딩**:
        - “해당 지역의 **거래 안정성**은 취소율 **{x%}**, 변동성 **{cv}**로 평가됩니다. 취소율 상승 시 **법적/운영 리스크**가 증가할 수 있습니다.”
- **페이지 D. Persona 탭(4종)**
    1. **연금형**: 안정적 현금흐름
        - **지표**: Yield, 취소율↓, 변동성↓
        - **뷰**: 지역별 Yield 박스플롯, 취소율 지도, 추천 리스트(환산가 기반)
        - **문구**: “현금흐름 안정성이 높은 순으로 제안합니다. 취소율이 낮고 Yield가 꾸준한 지역을 우선합니다.”
    2. **상업건물**: 밸류애드/규모
        - **지표**: ㎡당가, 건축년도, 층, 면적규모
        - **뷰**: ㎡당가 히트맵, 구축/신축 분포, 대형면적 필터
        - **문구**: “리모델링 여지가 있는 구축/저층 자산 위주로 밸류애드 기회를 탐색합니다.”
    3. **실거주 성장**: 주거 품질·실사용성
        - **지표**: 전용면적, 건축년도, 층, 가격대
        - **뷰**: 면적대 분포, 신축 프리미엄 곡선, 예산 슬라이더
        - **문구**: “실사용 면적, 준공연도, 층고·채광 등 ‘체감 품질’ 중심으로 필터링합니다.”
    4. **시세차익형**: 모멘텀·괴리
        - **지표**: 모멘텀(3M/6M), z-score(지역 대비), 변동성
        - **뷰**: 모멘텀 상위 랭킹, z-score 스캐터
        - **문구**: “단기 모멘텀이 강하고 지역 중위가 대비 저평가(z-score<0)인 후보를 표적화합니다.”
- **페이지 E. Explorer(원천 탐색)**
    - **필터**: 모든 기본 컬럼 + 파생지표
    - **그리드**: 원천 레코드 테이블(다운로드 버튼)
    - **워딩**: “필터를 조합해 원천 거래를 바로 탐색하고 내보낼 수 있습니다.”
- **페이지 F. Compare(비교)**
    - 후보 2~4개 선택 → **KPI 비교 카드** + **레이더 차트**
    - 워딩: “선택한 후보의 VC·RO 핵심 지표를 한 눈에 비교합니다.”

## 4.2 UX 마이크로카피(예시)

- **툴팁 – ㎡당가격**: “해당 거래의 ㎡당 환산 가격(만원). 지역 간 상대 비교에 사용합니다.”
- **툴팁 – 모멘텀**: “최근 3개월 평균 ㎡당가격이 이전 3개월 대비 얼마나 변했는지(%)”
- **툴팁 – Yield**: “연 임대수입(월세×12)을 매입가(또는 환산가)로 나눈 값(%)”
- **툴팁 – 취소율**: “해당 월 계약건 중 해제사유가 발생한 비율(%)”
- **경고 배지 – 둔화**: “거래량이 3M MA < 12M MA로 둔화 추세입니다.”

## 4.3 기술 스택 메모

- **Streamlit**: `st.tabs(["Overview","VC","RO","Persona","Explorer","Compare"])`
- **데이터 접근**: `sqlalchemy`로 MySQL 조회, 캐시(`st.cache_data`)
- **지도/시각화**: `pydeck` 또는 `folium`(지도), `matplotlib/plotly`(트렌드/산점도)
- **설정값 패널**: 환산계수 K, 이상치 컷오프, 가중치(VC 스코어) 등 사이드바에서 실시간 조정

---

# 5) 산출물 & 운영

- **산출물**:
    1. 정제 CSV(옵션) + MySQL DWH
    2. 파생지표 테이블/뷰(VC, RO, Persona별 랭커)
    3. Streamlit 대시보드(필터/다운로드 포함)
- **운영 루틴(예)**:
    - 일/주 단위로 CSV 업데이트 → `stg_*` 로드 → 품질검사(건수·결측·이상치) → `fact/dim` 리프레시 → 지표 재계산 → 대시보드 자동 반영

---

## (부록) 최소 동작 예시 체크리스트

1. **CSV → Pandas**: 타입 정리, 날짜·면적·가격 파생, 이상치 컷
2. **MySQL 적재**: `stg_*` → `fact_transactions` 머지
3. **지표 뷰**: 월별 가격/거래량/취소율/변동성/모멘텀/Yield 뷰 생성
4. **Streamlit 탭**: Overview/VC/RO/Persona/Explorer/Compare 구현
5. **설정패널**: K, 가중치, 필터 프리셋(연금형/상업건물/실거주/시세차익) 등록

---

원하시면, **Pandas 스크립트 템플릿**(파일 로드→정제→to_sql)과 **MySQL DDL/뷰 쿼리**, **Streamlit 기본 스켈레톤 코드**까지 한 번에 드리겠습니다.

추가로, 이번 스코프에서 **연립/단독**도 포함할지 여부만 말씀 주시면 같은 포맷으로 바로 확장해 드릴게요.