# 코드

좋아요! 말씀 주신 대로 **3~6년치 장기 데이터**를 전제로, **연립/단독까지 포함**하여 다음을 한 번에 드립니다:

1. **Pandas ETL 템플릿(파일 로드 → 정제/파생 → MySQL 적재)**
2. **MySQL DDL + 핵심 지표 VIEW 쿼리(월간 KPI/모멘텀/취소율/변동성/Yield)**
3. **Streamlit 대시보드 기본 스켈레톤 코드**
4. **대시보드 기획 설명(탭/지표/워딩/상호작용)**

필요 시 그대로 복붙해서 시작할 수 있도록 **경량/확장형 설계**로 구성했습니다.

---

# 0) 전제 & 폴더 구조(권장)

```
data/
  apt_trade/          # 아파트(매매)  CSV들
  apt_lease/          # 아파트(전월세) CSV들
  offi_trade/         # 오피스텔(매매) CSV들
  offi_lease/         # 오피스텔(전월세) CSV들
  comm_trade/         # 상업업무용(매매) CSV들
  row_trade/          # 연립다세대(매매) CSV들
  row_lease/          # 연립다세대(전월세) CSV들
  det_trade/          # 단독다가구(매매) CSV들
  det_lease/          # 단독다가구(전월세) CSV들

```

- 각 폴더에 **월 단위 CSV**(3~6년) 저장: `…_실거래가_YYYYMMDD.csv`
- 파일명에서 **유형(type)** 과 **스냅샷일자**를 추출해 증분 적재에 활용
- 법정동코드 테이블(최신본) 1개 별도 준비 권장: `ref_법정동코드.csv`

---

# 1) Pandas ETL 템플릿 (로드 → 정제 → 파생 → to_sql)

> 아래 코드는 여러 년치 CSV를 모두 읽어 표준 스키마로 정제하고, MySQL로 staging → fact/dim 순서로 적재합니다.
> 
> 
> (메모리 고려: `chunksize` 사용, dtype 지정, 지표는 추후 SQL View에서 계산)
> 

```python
# etl_realestate.py
import os, re, glob
import pandas as pd
import numpy as np
from datetime import datetime
from sqlalchemy import create_engine, text

# -----------------------
# 0) 설정
# -----------------------
MYSQL_URL = "mysql+pymysql://USER:PASS@HOST:3306/realestate?charset=utf8mb4"
engine = create_engine(MYSQL_URL, pool_recycle=3600, pool_pre_ping=True)

# 전월세 환산 계수 (튜닝 가능)
K_CONVERSION = 100

# 소스 폴더 매핑
SOURCE_DIRS = {
    "apt_trade": "data/apt_trade/*.csv",
    "apt_lease": "data/apt_lease/*.csv",
    "offi_trade": "data/offi_trade/*.csv",
    "offi_lease": "data/offi_lease/*.csv",
    "comm_trade": "data/comm_trade/*.csv",
    "row_trade":  "data/row_trade/*.csv",   # 연립다세대(매매)
    "row_lease":  "data/row_lease/*.csv",   # 연립다세대(전월세)
    "det_trade":  "data/det_trade/*.csv",   # 단독다가구(매매)
    "det_lease":  "data/det_lease/*.csv",   # 단독다가구(전월세)
}

# 파일명에서 스냅샷일자 추출 (예: ..._20250930.csv)
DATE_IN_NAME = re.compile(r'(\d{8})')

# 공통 표준 스키마 컬럼명 (원천 컬럼명의 다양한 변형을 흡수)
COLMAP = {
    "시도": ["시도", "광역시도", "광역시/도"],
    "시군구": ["시군구", "시/군/구"],
    "읍면동": ["읍면동", "법정동", "동"],
    "법정동코드": ["법정동코드", "법정동 코드", "법정코드"],

    "건축년도": ["건축년도", "건축 연도", "준공년도"],
    "층": ["층", "해당층"],
    "전용면적_㎡": ["전용면적(㎡)", "전용면적", "면적(㎡)"],
    "거래금액_만원": ["거래금액(만원)", "거래금액", "금액(만원)", "매매금액(만원)"],
    "보증금_만원": ["보증금(만원)", "보증금"],
    "월세_만원": ["월세(만원)", "월세"],
    "계약년월": ["계약년월", "년월"],
    "계약일": ["계약일", "일"],
    "해제사유발생일": ["해제사유발생일", "해제일", "해제 발생일"],
}

# -----------------------
# 1) 유틸
# -----------------------
def stdcol(df):
    # 원천의 다양한 한글 컬럼명을 표준 컬럼으로 통일
    colmap_rev = {}
    for std, alts in COLMAP.items():
        for a in alts:
            colmap_rev[a] = std
    newcols = {}
    for c in df.columns:
        newcols[c] = colmap_rev.get(c, c)
    return df.rename(columns=newcols)

def to_num(x):
    if pd.isna(x): return np.nan
    s = str(x).replace(',', '').replace(' ', '')
    s = re.sub(r'[^0-9\.-]', '', s)
    try:
        return float(s)
    except:
        return np.nan

def build_date(yyyymm, dd):
    if pd.isna(yyyymm): return pd.NaT
    yyyymm = int(yyyymm)
    y = yyyymm // 100
    m = yyyymm % 100
    d = int(dd) if pd.notna(dd) else 1
    try:
        return pd.Timestamp(y, m, d)
    except:
        # 유효하지 않은 일자 → 월말로 보정
        return (pd.Timestamp(y, m, 1) + pd.offsets.MonthEnd(0))

def detect_snapshot_from_filename(path):
    m = DATE_IN_NAME.search(os.path.basename(path))
    if m:
        return pd.to_datetime(m.group(1), format='%Y%m%d', errors='coerce')
    return pd.NaT

def normalize_and_derive(df, src_type):
    # 컬럼명 통일
    df = stdcol(df)

    # 숫자형 변환
    for c in ["층","건축년도","전용면적_㎡","거래금액_만원","보증금_만원","월세_만원","계약년월","계약일"]:
        if c in df.columns:
            df[c] = df[c].apply(to_num)

    # 날짜/파생
    if "계약년월" in df.columns:
        df["계약일자"] = df.apply(lambda r: build_date(r.get("계약년월"), r.get("계약일")), axis=1)
        df["YYYYMM"] = df["계약일자"].dt.strftime("%Y%m").astype("Int64")
        df["연도"] = df["계약일자"].dt.year.astype("Int64")
        df["월"] = df["계약일자"].dt.month.astype("Int64")
        df["분기"] = df["계약일자"].dt.quarter.astype("Int64")

    # 면적 파생
    if "전용면적_㎡" in df.columns:
        df["면적_평"] = df["전용면적_㎡"] * 0.3025

    # ㎡당/평당 가격
    if "거래금액_만원" in df.columns and "전용면적_㎡" in df.columns:
        df["가격_per_㎡"] = (df["거래금액_만원"] / df["전용면적_㎡"]).replace([np.inf, -np.inf], np.nan)
        df["가격_per_평"] = (df["거래금액_만원"] / df["면적_평"]).replace([np.inf, -np.inf], np.nan)

    # 전월세 환산 & Yield
    if "보증금_만원" in df.columns or "월세_만원" in df.columns:
        df["환산가_만원"] = (df.get("보증금_만원", 0).fillna(0) + df.get("월세_만원", 0).fillna(0) * K_CONVERSION)
        # 추정매입가: 매매가 있으면 그 값, 없으면 환산가
        df["추정매입가_만원"] = np.where(df.get("거래금액_만원").notna(), df["거래금액_만원"], df["환산가_만원"])
        df["연임대수입_만원"] = df.get("월세_만원", 0).fillna(0) * 12
        df["Yield_%"] = (df["연임대수입_만원"] / df["추정매입가_만원"]) * 100

    # 취소 플래그
    if "해제사유발생일" in df.columns:
        df["해제사유발생일"] = pd.to_datetime(df["해제사유발생일"], errors="coerce")
        df["취소여부"] = df["해제사유발생일"].notna().astype(int)
    else:
        df["취소여부"] = 0

    # 이상치(윈저라이즈): 유형/연도/면적버킷 기준 상하위 1% 컷
    if {"전용면적_㎡","거래금액_만원"}.issubset(df.columns):
        grp = df.groupby(pd.cut(df["전용면적_㎡"], bins=[0, 30, 60, 85, 135, 1e6]))
        def winsor(s):
            q01, q99 = s.quantile([0.01, 0.99])
            return s.clip(lower=q01, upper=q99)
        df["거래금액_만원"] = grp["거래금액_만원"].transform(winsor)

    # 소스 타입 기록
    df["src_type"] = src_type
    return df

def read_all_into_staging():
    frames = []
    for src_type, pattern in SOURCE_DIRS.items():
        for fp in glob.glob(pattern):
            try:
                snap = detect_snapshot_from_filename(fp)
                # 빠른 로딩: infer=False, dtype 지정은 상황에 맞게 보강 가능
                df = pd.read_csv(fp, low_memory=False)
                df["snapshot_date"] = snap
                df = normalize_and_derive(df, src_type)
                frames.append(df)
            except Exception as e:
                print(f"[WARN] {src_type} load failed: {fp} ({e})")
    if not frames:
        return pd.DataFrame()
    df = pd.concat(frames, ignore_index=True)

    # 중복 제거: (지역키 + 계약일자 + 면적 + 금액 + src_type)
    subset_cols = [c for c in ["시도","시군구","읍면동","법정동코드","계약일자","전용면적_㎡","거래금액_만원","src_type"] if c in df.columns]
    if subset_cols:
        df = df.drop_duplicates(subset=subset_cols)
    return df

def load_dim_geo():
    # 별도 참조 테이블을 CSV 등으로부터 로드 (선택)
    # 여기서는 테이블이 이미 존재한다고 가정. 필요시 upsert 로직 추가.
    pass

def upsert_staging(df):
    # stg_transactions: 원본 보존 + 표준화 컬럼
    cols = [
        "src_type","snapshot_date","시도","시군구","읍면동","법정동코드","건축년도","층",
        "전용면적_㎡","면적_평","거래금액_만원","보증금_만원","월세_만원",
        "계약년월","계약일","계약일자","YYYYMM","연도","월","분기",
        "가격_per_㎡","가격_per_평","환산가_만원","추정매입가_만원","연임대수입_만원","Yield_%",
        "해제사유발생일","취소여부"
    ]
    exists = [c for c in cols if c in df.columns]
    df_to_sql = df[exists].copy()
    df_to_sql.to_sql("stg_transactions", engine, if_exists="append", index=False, chunksize=5000, method="multi")

def build_dwh_with_sql():
    with engine.begin() as conn:
        # dim_time
        conn.execute(text("""
        INSERT INTO dim_time (date_key, date, year, month, quarter, yyyymm)
        SELECT DISTINCT
          DATE_FORMAT(계약일자, '%Y%m%d')+0 AS date_key,
          계약일자 AS date,
          YEAR(계약일자) AS year,
          MONTH(계약일자) AS month,
          QUARTER(계약일자) AS quarter,
          DATE_FORMAT(계약일자, '%Y%m')+0 AS yyyymm
        FROM stg_transactions st
        WHERE 계약일자 IS NOT NULL
        ON DUPLICATE KEY UPDATE date=date;"""))

        # dim_geo (간략)
        conn.execute(text("""
        INSERT INTO dim_geo (geo_hash, sido, sigungu, eupmyeondong, beop_code)
        SELECT DISTINCT
            MD5(CONCAT(coalesce(시도,''),'|',coalesce(시군구,''),'|',coalesce(읍면동,''),'|',coalesce(법정동코드,''))) AS geo_hash,
            시도, 시군구, 읍면동, 법정동코드
        FROM stg_transactions
        ON DUPLICATE KEY UPDATE sido=VALUES(sido), sigungu=VALUES(sigungu), eupmyeondong=VALUES(eupmyeondong), beop_code=VALUES(beop_code);
        """))

        # dim_building (간략)
        conn.execute(text("""
        INSERT INTO dim_building (bld_hash, built_year, floor)
        SELECT DISTINCT
            MD5(CONCAT(coalesce(CAST(건축년도 AS CHAR),''),'|',coalesce(CAST(층 AS CHAR),''))) AS bld_hash,
            건축년도, 층
        FROM stg_transactions
        ON DUPLICATE KEY UPDATE built_year=VALUES(built_year), floor=VALUES(floor);
        """))

        # fact_transactions (업서트)
        conn.execute(text("""
        INSERT INTO fact_transactions (
            src_type, snapshot_date, geo_id, time_id, building_id,
            area_m2, area_pyeong, floor, built_year,
            price_10k, deposit_10k, rent_10k,
            deal_date, yyyymm, cancel_flag,
            p_per_m2, p_per_pyeong, conv_10k, est_price_10k, ann_rent_10k, yield_pct
        )
        SELECT
            st.src_type, st.snapshot_date,
            g.id AS geo_id,
            t.id AS time_id,
            b.id AS building_id,
            st.전용면적_㎡, st.면적_평, st.층, st.건축년도,
            st.거래금액_만원, st.보증금_만원, st.월세_만원,
            st.계약일자, st.YYYYMM, st.취소여부,
            st.가격_per_㎡, st.가격_per_평, st.환산가_만원, st.추정매입가_만원, st.연임대수입_만원, st.Yield_%
        FROM stg_transactions st
        JOIN dim_geo g ON g.geo_hash = MD5(CONCAT(coalesce(st.시도,''),'|',coalesce(st.시군구,''),'|',coalesce(st.읍면동,''),'|',coalesce(st.법정동코드,'')))
        JOIN dim_time t ON t.date = st.계약일자
        JOIN dim_building b ON b.built_year <=> st.건축년도 AND b.floor <=> st.층
        ON DUPLICATE KEY UPDATE
          snapshot_date=VALUES(snapshot_date),
          price_10k=VALUES(price_10k),
          deposit_10k=VALUES(deposit_10k),
          rent_10k=VALUES(rent_10k),
          yield_pct=VALUES(yield_pct),
          p_per_m2=VALUES(p_per_m2),
          p_per_pyeong=VALUES(p_per_pyeong),
          est_price_10k=VALUES(est_price_10k),
          conv_10k=VALUES(conv_10k),
          ann_rent_10k=VALUES(ann_rent_10k),
          cancel_flag=VALUES(cancel_flag);
        """))

if __name__ == "__main__":
    df = read_all_into_staging()
    if len(df) == 0:
        print("No data loaded.")
    else:
        upsert_staging(df)
        build_dwh_with_sql()
        print(f"Loaded rows: {len(df)}")

```

---

# 2) MySQL DDL (스키마 + 인덱스 + 파티셔닝 예시) & VIEW

> 주의: MySQL 8 기준. 파티셔닝은 설치 환경에 맞춰 사용. 업서트를 위해 적절한 Unique Key 설계가 필요합니다(예: (src_type, geo_id, time_id, area_m2, price_10k) 등 비즈 규칙에 맞게 조정).
> 

```sql
-- 2.1 스키마
CREATE DATABASE IF NOT EXISTS realestate DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
USE realestate;

-- staging
CREATE TABLE IF NOT EXISTS stg_transactions (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  src_type VARCHAR(32),               -- apt_trade / apt_lease / offi_trade / offi_lease / comm_trade / row_* / det_*
  snapshot_date DATE NULL,

  시도 VARCHAR(32), 시군구 VARCHAR(32), 읍면동 VARCHAR(32), 법정동코드 VARCHAR(20),
  건축년도 INT NULL, 층 INT NULL,
  전용면적_㎡ DECIMAL(10,4), 면적_평 DECIMAL(10,4),
  거래금액_만원 INT NULL, 보증금_만원 INT NULL, 월세_만원 INT NULL,

  계약년월 INT NULL, 계약일 INT NULL, 계약일자 DATE NULL,
  YYYYMM INT NULL, 연도 INT NULL, 월 INT NULL, 분기 INT NULL,

  가격_per_㎡ DECIMAL(14,6) NULL, 가격_per_평 DECIMAL(14,6) NULL,
  환산가_만원 INT NULL, 추정매입가_만원 INT NULL, 연임대수입_만원 INT NULL, Yield_% DECIMAL(8,4) NULL,

  해제사유발생일 DATE NULL, 취소여부 TINYINT(1) DEFAULT 0,

  KEY idx_src_ym (src_type, YYYYMM),
  KEY idx_geo (시도, 시군구, 읍면동),
  KEY idx_date (계약일자)
);

-- dimensions
CREATE TABLE IF NOT EXISTS dim_time (
  id INT AUTO_INCREMENT PRIMARY KEY,
  date_key INT UNIQUE,
  date DATE NOT NULL,
  year INT, month INT, quarter INT, yyyymm INT,
  KEY idx_ym (yyyymm)
);

CREATE TABLE IF NOT EXISTS dim_geo (
  id INT AUTO_INCREMENT PRIMARY KEY,
  geo_hash CHAR(32) UNIQUE,
  sido VARCHAR(32), sigungu VARCHAR(32), eupmyeondong VARCHAR(32), beop_code VARCHAR(20),
  KEY idx_region (sido, sigungu, eupmyeondong)
);

CREATE TABLE IF NOT EXISTS dim_building (
  id INT AUTO_INCREMENT PRIMARY KEY,
  bld_hash CHAR(32) UNIQUE,
  built_year INT NULL, floor INT NULL,
  KEY idx_built (built_year),
  KEY idx_floor (floor)
);

-- facts
CREATE TABLE IF NOT EXISTS fact_transactions (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  src_type VARCHAR(32) NOT NULL,
  snapshot_date DATE NULL,
  geo_id INT NOT NULL, time_id INT NOT NULL, building_id INT NULL,

  area_m2 DECIMAL(10,4), area_pyeong DECIMAL(10,4),
  floor INT, built_year INT,

  price_10k INT, deposit_10k INT, rent_10k INT,
  deal_date DATE, yyyymm INT, cancel_flag TINYINT(1) DEFAULT 0,

  p_per_m2 DECIMAL(14,6), p_per_pyeong DECIMAL(14,6),
  conv_10k INT, est_price_10k INT, ann_rent_10k INT, yield_pct DECIMAL(8,4),

  -- 업서트를 위한 자연키(비즈 규칙에 맞게 조정 권장)
  UNIQUE KEY uk_natural (src_type, geo_id, time_id, IFNULL(price_10k,0), IFNULL(deposit_10k,0), IFNULL(rent_10k,0), IFNULL(area_m2,0)),
  KEY idx_src_ym (src_type, yyyymm),
  KEY idx_geo_ym (geo_id, yyyymm),
  KEY idx_deal (deal_date)
)
-- 파티셔닝 예시(YYYYMM 기준). 환경에 맞게 조정/생략 가능.
-- PARTITION BY RANGE (yyyymm) (
--   PARTITION p2023m01 VALUES LESS THAN (202302),
--   PARTITION pmax VALUES LESS THAN MAXVALUE
-- )
;

-- 2.2 KPI 뷰들

-- 월간 집계 기본(가격, 거래량)
CREATE OR REPLACE VIEW vw_monthly_basics AS
SELECT
  f.src_type,
  g.sido, g.sigungu, g.eupmyeondong,
  t.yyyymm,
  COUNT(*) AS txn_cnt,
  AVG(f.p_per_m2) AS avg_p_per_m2,
  STDDEV_POP(f.p_per_m2) AS std_p_per_m2,
  AVG(f.yield_pct) AS avg_yield_pct,
  SUM(f.cancel_flag) / COUNT(*) AS cancel_rate
FROM fact_transactions f
JOIN dim_geo g ON g.id = f.geo_id
JOIN dim_time t ON t.id = f.time_id
GROUP BY 1,2,3,4,5;

-- 모멘텀(최근3M/이전3M)
-- 간단화를 위해 동일 뷰 내 self join 대신 윈도우 함수를 써도 됨(8.0+)
CREATE OR REPLACE VIEW vw_monthly_momentum AS
SELECT
  src_type, sido, sigungu, eupmyeondong, yyyymm,
  avg_p_per_m2,
  LAG(avg_p_per_m2, 3) OVER (PARTITION BY src_type, sido, sigungu, eupmyeondong ORDER BY yyyymm) AS avg_p_per_m2_lag3,
  CASE
    WHEN LAG(avg_p_per_m2, 3) OVER (PARTITION BY src_type, sido, sigungu, eupmyeondong ORDER BY yyyymm) IS NULL
    THEN NULL
    ELSE (avg_p_per_m2 /
          LAG(avg_p_per_m2, 3) OVER (PARTITION BY src_type, sido, sigungu, eupmyeondong ORDER BY yyyymm) - 1)
  END AS mom_3m
FROM vw_monthly_basics;

-- 변동계수(CV) 월별
CREATE OR REPLACE VIEW vw_monthly_volatility AS
SELECT
  src_type, sido, sigungu, eupmyeondong, yyyymm,
  std_p_per_m2 / NULLIF(avg_p_per_m2,0) AS cv_p_per_m2
FROM vw_monthly_basics;

-- 임대형(전월세) Yield 분포
CREATE OR REPLACE VIEW vw_yield_dist AS
SELECT
  src_type, sido, sigungu, eupmyeondong, yyyymm,
  AVG(yield_pct) AS avg_yield, PERCENTILE_CONT(0.25) WITHIN GROUP (ORDER BY yield_pct) OVER w AS q1,
  PERCENTILE_CONT(0.5)  WITHIN GROUP (ORDER BY yield_pct) OVER w AS median_yield,
  PERCENTILE_CONT(0.75) WITHIN GROUP (ORDER BY yield_pct) OVER w AS q3
FROM fact_transactions f
JOIN dim_geo g ON g.id = f.geo_id
JOIN dim_time t ON t.id = f.time_id
WINDOW w AS (PARTITION BY src_type, sido, sigungu, eupmyeondong, yyyymm);

```

---

# 3) Streamlit 기본 스켈레톤

> 요지: 사이드바에서 지역/유형/기간/K값/가중치를 고르고, 탭별로 Overview / VC / RO / Persona(4) / Explorer / Compare 제공.
> 

```python
# app.py
import streamlit as st
import pandas as pd
import numpy as np
from sqlalchemy import create_engine

MYSQL_URL = "mysql+pymysql://USER:PASS@HOST:3306/realestate?charset=utf8mb4"
engine = create_engine(MYSQL_URL, pool_recycle=3600, pool_pre_ping=True)

st.set_page_config(page_title="부동산 AI Dashboard", layout="wide")

# -----------------------
# Sidebar Filters
# -----------------------
st.sidebar.header("필터")
src_map = {
    "아파트(매매)": "apt_trade",
    "아파트(전월세)": "apt_lease",
    "오피스텔(매매)": "offi_trade",
    "오피스텔(전월세)": "offi_lease",
    "상업업무용(매매)": "comm_trade",
    "연립다세대(매매)": "row_trade",
    "연립다세대(전월세)": "row_lease",
    "단독다가구(매매)": "det_trade",
    "단독다가구(전월세)": "det_lease",
}
src_label = st.sidebar.selectbox("유형", list(src_map.keys()))
src_type = src_map[src_label]

sido = st.sidebar.text_input("시도(예: 서울특별시)", "")
sigungu = st.sidebar.text_input("시군구(예: 강남구)", "")
date_from = st.sidebar.text_input("시작 YYYYMM (예: 202201)", "")
date_to   = st.sidebar.text_input("종료 YYYYMM (예: 202512)", "")

K = st.sidebar.number_input("전월세 환산 K", min_value=50, max_value=200, value=100, step=5)

st.sidebar.markdown("---")
st.sidebar.caption("※ 필터 미입력 시 전체 조회(주의: 대용량)")

# -----------------------
# Query Helpers
# -----------------------
def load_monthly_basics(src_type, sido=None, sigungu=None, ym_from=None, ym_to=None):
    q = f"""
    SELECT * FROM vw_monthly_basics
    WHERE src_type = :src
    """
    params = {"src": src_type}
    if sido:
        q += " AND sido = :sido"
        params["sido"] = sido
    if sigungu:
        q += " AND sigungu = :sigungu"
        params["sigungu"] = sigungu
    if ym_from:
        q += " AND yyyymm >= :ymf"
        params["ymf"] = int(ym_from)
    if ym_to:
        q += " AND yyyymm <= :ymt"
        params["ymt"] = int(ym_to)
    q += " ORDER BY yyyymm"
    return pd.read_sql(q, engine, params=params)

def load_momentum(src_type, sido=None, sigungu=None, ym_from=None, ym_to=None):
    q = f"SELECT * FROM vw_monthly_momentum WHERE src_type=:src"
    params = {"src": src_type}
    if sido: q += " AND sido=:sido"; params["sido"]=sido
    if sigungu: q += " AND sigungu=:sigungu"; params["sigungu"]=sigungu
    if ym_from: q += " AND yyyymm>=:ymf"; params["ymf"]=int(ym_from)
    if ym_to: q += " AND yyyymm<=:ymt"; params["ymt"]=int(ym_to)
    q += " ORDER BY yyyymm"
    return pd.read_sql(q, engine, params=params)

def load_explorer(src_type, sido=None, sigungu=None, ym_from=None, ym_to=None, limit=5000):
    q = """
    SELECT f.*, g.sido, g.sigungu, g.eupmyeondong, t.yyyymm
    FROM fact_transactions f
    JOIN dim_geo g ON g.id=f.geo_id
    JOIN dim_time t ON t.id=f.time_id
    WHERE f.src_type=:src
    """
    params = {"src": src_type}
    if sido: q += " AND g.sido=:sido"; params["sido"]=sido
    if sigungu: q += " AND g.sigungu=:sigungu"; params["sigungu"]=sigungu
    if ym_from: q += " AND t.yyyymm>=:ymf"; params["ymf"]=int(ym_from)
    if ym_to: q += " AND t.yyyymm<=:ymt"; params["ymt"]=int(ym_to)
    q += " ORDER BY t.yyyymm LIMIT :lim"
    params["lim"] = int(limit)
    return pd.read_sql(q, engine, params=params)

# -----------------------
# Tabs
# -----------------------
tab_overview, tab_vc, tab_ro, tab_persona, tab_explorer, tab_compare = st.tabs(
    ["Overview", "VC", "RO", "Persona", "Explorer", "Compare"]
)

with tab_overview:
    st.subheader("개요 (월간 KPI)")
    dfm = load_monthly_basics(src_type, sido, sigungu, date_from or None, date_to or None)
    if len(dfm)==0:
        st.info("데이터 없음. 필터를 조정하세요.")
    else:
        c1, c2, c3, c4 = st.columns(4)
        c1.metric("평균 ㎡당가(만원)", f"{dfm['avg_p_per_m2'].iloc[-1]:.1f}")
        c2.metric("거래량(최근월)", int(dfm['txn_cnt'].iloc[-1]))
        c3.metric("평균 Yield(%)", f"{(dfm['avg_yield_pct'].iloc[-1] or 0):.2f}")
        c4.metric("취소율(최근월)", f"{(dfm['cancel_rate'].iloc[-1]*100 if dfm['cancel_rate'].iloc[-1] is not None else 0):.2f}%")
        st.line_chart(dfm.set_index("yyyymm")[["avg_p_per_m2","txn_cnt"]])

with tab_vc:
    st.subheader("VC 랩 (추천/랭킹)")
    st.caption("가격 경쟁력(㎡당가), 품질(준공/층), 모멘텀 가중합으로 스코어화")
    dfm = load_momentum(src_type, sido, sigungu, date_from or None, date_to or None)
    if len(dfm):
        st.area_chart(dfm.set_index("yyyymm")[["avg_p_per_m2"]])
    st.info("상세 랭킹/추천 로직은 추후 SQL/파이썬 뷰로 연결 (가중치 슬라이더 제공 가능).")

with tab_ro:
    st.subheader("RO 보드 (리스크&운영)")
    dfm = load_monthly_basics(src_type, sido, sigungu, date_from or None, date_to or None)
    if len(dfm):
        dfv = pd.read_sql(
            "SELECT * FROM vw_monthly_volatility WHERE src_type=:src ORDER BY yyyymm",
            engine, params={"src":src_type}
        )
        st.line_chart(dfv.set_index("yyyymm")[["cv_p_per_m2"]])
        st.bar_chart(dfm.set_index("yyyymm")[["cancel_rate"]])

with tab_persona:
    st.subheader("Persona 보드")
    ptab1, ptab2, ptab3, ptab4 = st.tabs(["연금형","상업건물","실거주 성장","시세차익형"])
    with ptab1:
        st.caption("연금형: Yield↑, 변동성↓, 취소율↓")
        # 예시: 상위 Yield 지역 테이블 (간단히 최근월 기준)
        df = load_monthly_basics(src_type, sido, sigungu, date_from or None, date_to or None)
        if len(df):
            top = df.sort_values("avg_yield_pct", ascending=False).head(20)
            st.dataframe(top[["sido","sigungu","eupmyeondong","yyyymm","avg_yield_pct","cancel_rate"]])
    with ptab2:
        st.caption("상업건물: ㎡당가 & 구축/층 특성으로 밸류애드 탐색")
        st.info("상업/준상업 유형일 때 가중치/필터 강화 (건축년도 낮음 + 저층)")
    with ptab3:
        st.caption("실거주 성장: 면적/준공/층 + 예산 필터")
        st.info("예산/면적대 슬라이더 제공, 신축 프리미엄 뷰 연결")
    with ptab4:
        st.caption("시세차익형: 모멘텀↑, 지역 대비 z-score<0 후보")
        st.info("모멘텀 상위/저평가 지도 및 스캐터 계획")

with tab_explorer:
    st.subheader("Explorer (원천 레코드)")
    limit = st.number_input("최대 행 수", value=2000, step=100)
    df = load_explorer(src_type, sido, sigungu, date_from or None, date_to or None, limit=int(limit))
    st.dataframe(df)

with tab_compare:
    st.subheader("Compare (후보 비교)")
    st.info("후보 선택 → KPI 카드/레이더 차트로 VC/RO 비교 (후속 구현)")

```

---

# 4) 대시보드 기획 설명 (VC/RO/Persona 관점 워딩 포함)

## (1) 탭 구조와 목적

- **Overview**: 선택 지역/유형의 **핵심 KPI**(㎡당가, 거래량, Yield, 취소율, 변동성) 추세를 한눈에.
    - *워딩 예*: “최근 3개월 평균 ㎡당가격은 **{X}만원**으로 전월 대비 **{±Y%}**입니다. 거래 취소율은 **{Z%}**로 최근 12개월 평균 대비 **{상승/하락}**했습니다.”
- **VC**: **Value & Choice** 관점의 추천/랭킹.
    - 스코어 = **가격경쟁력(40%) + 품질(준공/층 30%) + 모멘텀(30%)** (사이드바에서 가중치 조절)
    - *워딩*: “선택 조건에서 상위 10개 후보를 제안합니다.”
- **RO**: **Risk & Ops** 관점의 리스크 보드.
    - 지표: **취소율**, **거래량 둔화(3M MA vs 12M MA)**, **변동계수(CV)**
    - *배지 예*: “거래량이 둔화 추세입니다(3M MA < 12M MA).”
- **Persona(4)**:
    1. **연금형**: Yield↑/취소율↓/변동성↓ 우선
    2. **상업건물**: ㎡당가/건축년도/층/면적 중심 밸류애드
    3. **실거주 성장**: 전용면적·준공·층·예산 기반 체감 품질
    4. **시세차익형**: 모멘텀 상위 + 지역 대비 저평가(z-score)
    - *툴팁 예*:
        - **Yield**: “연 임대수입(월세×12)/매입가(또는 환산가)”
        - **모멘텀**: “최근 3개월 평균 ㎡당가격 / 이전 3개월 - 1”
        - **취소율**: “해당 월 계약건 중 해제사유 발생 비율”
- **Explorer**: 필터링된 **원천 레코드 탐색/다운로드**.
- **Compare**: 선택 후보 2~4개 **KPI 카드 + 레이더 차트**로 비교.

## (2) 장기데이터(3~6년) 운영 포인트

- **성능**: `fact_transactions`는 **YYYYMM 인덱스/파티션** 기준, 월간 뷰 쿼리 가속.
- **증분 로드**: 파일명 스냅샷일자 기반으로 **신규/갱신만 적재**.
- **파라미터화**: 환산계수(K), 이상치 컷, VC 가중치 → 사이드바에서 **즉시 반영**.
- **유형 확장**: 연립/단독(매매/전월세)도 동일 파이프라인 & 대시보드에 그대로 연결.

---

# 5) VC/RO/Persona용 지표 계산(확장 아이디어)

- **VC**:
    - 가격 경쟁력: `지역중위 대비 (중위 ㎡당가 - 개별 ㎡당가) / 중위 ㎡당가`
    - 품질 프리미엄: `신축(예: 10년 이내) 가산`, `중층/로열층 가산`
    - 모멘텀: `mom_3m` (뷰 활용)
- **RO**:
    - 취소율: `월 취소건 / 월 계약건`
    - 거래량 둔화 신호: `3M MA < 12M MA`
    - 변동성: `CV = std/avg`
- **Persona**:
    - 연금형: `Score = w1*Yield - w2*CV - w3*취소율`
    - 상업건물: `Score = w1*(저평가도) + w2*(밸류애드 지수: 구축+저층) + w3*(면적규모)`
    - 실거주 성장: `Score = w1*(면적/예산 적합) + w2*(준공 프리미엄) + w3*(층 프리미엄)`
    - 시세차익형: `Score = w1*mom_3m + w2*(-zscore_가격)`

---

## 바로 실행 팁

- **1회 세팅**: `pip install pandas numpy SQLAlchemy PyMySQL streamlit`
- **DB 준비**: 위 DDL 실행 → `python etl_realestate.py` → `streamlit run app.py`
- **환경별 조정**:
    - CSV 인코딩/구분자 이슈 시 `pd.read_csv(..., encoding='cp949')` 등 적용
    - MySQL 버전에 따라 `PERCENTILE_CONT` 지원이 다를 수 있으니, 필요하면 서브쿼리/윈도우 대체

---

원하시면 위 코드들을 **파일로 분리(.py/.sql)** 해서 드리거나, **지표 View를 더 촘촘히**(z-score, 6M 모멘텀, 렌트 vs 매입 갭 등) 확장한 버전도 만들어 드릴게요.